1. SSH 키란?
   해당프로젝트는 Gitlab과 Jenkins를 연동하기 위해 SSH키를 사용한다. SSH란 Secure Shell의 준말로 원격지 호스트 컴퓨터에서 접속하기 위해 사용되는 프로토콜이며, 보안 쉘을 의마합니다.
   SSH의 기본 포트는 22번을 사용한다
   SSH키는 SSH Client 와 SSH Sever 사이에서 key를 통해 접속하며, 비밀번호보다 높으 보안 수준을 지닌다.


2. SSH가 동작하는 방식 
  2-1) SSH 키는 공개기(public key)와 비공개(private key)로 구성된다. 개인키(private key)는 본인만 가지고 있는 키를 의미하며, 공개키(public key)는 상대방에게 공개하는 키를 의미한다.
  
